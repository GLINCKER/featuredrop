# featuredrop docs app

Nextra-powered docs and marketing site deployed on Vercel.

## Local development

```bash
pnpm install
pnpm dev
```

## Build

```bash
pnpm build
```

Use the root scripts from repository root:

- `pnpm docs:install`
- `pnpm docs:dev`
- `pnpm docs:build`
